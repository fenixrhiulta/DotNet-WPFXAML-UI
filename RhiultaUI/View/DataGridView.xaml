<UserControl x:Class="RhiultaUI.View.DataGridView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:RhiultaUI.View"
             xmlns:RhiultaUI="clr-namespace:RhiultaUI"
             mc:Ignorable="d" d:Background="#EEE"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <DataGrid ItemsSource="{Binding model.Lista1}">
            <DataGrid.Columns>
                <DataGridTextColumn Width="120" Header="CÓDIGO" Binding="{Binding codigobarras}" />
                <DataGridTextColumn Width="*" Header="NOME" Binding="{Binding nome}" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
